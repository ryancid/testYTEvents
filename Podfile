# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'testYTEvents' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for testYTEvents
  
  pod 'YoutubePlayer-in-WKWebView', :git => 'https://github.com/ryancid/YoutubePlayer-in-WKWebView.git', :branch => 'onError'
end
